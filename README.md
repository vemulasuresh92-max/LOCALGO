# LOCALGO
Find Local. Book Local. Grow Local.

This is the root Flutter customer app project for LOCALGO.

## Google Play build
Codemagic reads `codemagic.yaml`, creates Android platform files when needed, and builds a release Android App Bundle (`.aab`).

For a production Google Play release, configure an Android release keystore/signing credentials in Codemagic before publishing.

## Package ID
`com.localgo.customer`

## Version
`1.0.0+1`
