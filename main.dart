import 'package:flutter/material.dart';
void main()=>runApp(const App());
class App extends StatelessWidget{const App({super.key});@override Widget build(BuildContext c)=>MaterialApp(debugShowCheckedModeBanner:false,title:'LOCALGO Business',theme:ThemeData(useMaterial3:true,colorSchemeSeed:const Color(0xFF155EEF)),home:const Dashboard());}
class Dashboard extends StatelessWidget{const Dashboard({super.key});@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('LOCALGO Business')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Manage Local. Grow Local.',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const SizedBox(height:20),const Card(child:ListTile(title:Text('New Leads'),trailing:Text('0'))),const Card(child:ListTile(title:Text('Bookings'),trailing:Text('0'))),const Card(child:ListTile(title:Text('Completed'),trailing:Text('0'))),const SizedBox(height:20),const ListTile(leading:Icon(Icons.add_business),title:Text('Add Service')),const ListTile(leading:Icon(Icons.calendar_month),title:Text('Manage Bookings'))]),bottomNavigationBar:const NavigationBar(destinations:[NavigationDestination(icon:Icon(Icons.dashboard),label:'Dashboard'),NavigationDestination(icon:Icon(Icons.inbox),label:'Leads'),NavigationDestination(icon:Icon(Icons.calendar_month),label:'Bookings'),NavigationDestination(icon:Icon(Icons.build),label:'Services'),NavigationDestination(icon:Icon(Icons.settings),label:'Settings')]));}
